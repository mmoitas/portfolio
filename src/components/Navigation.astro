---
export interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<nav class:list={['navigation', className]} aria-label="Primary">
	<div class="left-actions">
		<button
			type="button"
			class="nav-button"
			aria-haspopup="true"
			aria-expanded="false"
			aria-label="Open menu"
		>
			<img src="/images/icons/nav-small.svg" alt="" class="nav-icon" width="24" height="24" />
			<img src="/images/icons/head-medium.svg" alt="" class="site-icon" width="48" height="48" />
		</button>
		<!-- <a href="/" class="site-name">
			<img src="/images/icons/head-medium.svg" alt="" class="site-icon" width="48" height="48" />
			<p class="text-nav-title"><slot>Marcos Moitas</slot></p>
		</a> -->
	</div>
</nav>

<style>
	.navigation {
		display: flex;
		align-items: center;
		width: 100%;
		padding: var(--space-4) var(--space-10);
		background-color: var(--surface-color-0);
	}

	.left-actions {
		display: flex;
		align-items: center;
		gap: var(--space-2);
	}

	.nav-button {
		display: flex;
		align-items: center;
		padding: 0;
		border: none;
		background: none;
		cursor: pointer;
		gap: var(--space-2);
		background-color: var(--neutral-2);
	}

	.nav-icon {
		display: block;
	}

	.site-name {
		display: flex;
		align-items: center;
		gap: var(--space-2);
		text-decoration: none;
	}

	.site-icon {
		display: block;
	}
</style>
